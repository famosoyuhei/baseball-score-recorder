# Scripts 作業ガイド

## 範囲

`scripts/` は静的サーバー、ビルド、AI従業員モード検証などの補助処理を置く。

## キャッシュ

アプリ配信物や実行時コードに影響するスクリプトを変更したら、`../sw.js` の `CACHE_NAME` 更新が必要か確認する。文書生成や検証専用スクリプトだけの変更なら不要。

## 検証

- 開発サーバー: `npm start`
- ビルド: `npm run build`
- AI従業員モード検証: `npm run test:ai-matrix`
