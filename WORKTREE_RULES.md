# Worktree 運用ルール

## 基本方針

**常設の機能別ワークツリーは作らない。**  
1タスク＝1使い捨てワークツリーを標準とする。

このプロジェクトは `app.js` / `game.js` / `i18n.js` / `style.css` が密結合した単一PWAであり、
機能別の常設ブランチを維持してもマージコストが増すだけでメリットがない。

---

## ブランチ命名規則

| 種別 | プレフィックス | 対象ファイルの目安 |
|---|---|---|
| 新機能 | `feat/*` | js/app.js, js/game.js など |
| バグ修正 | `fix/*` | 任意 |
| 翻訳追加・修正 | `i18n/*` | js/i18n.js のみ |
| CSS修正 | `style/*` | css/style.css のみ |

---

## 作業フロー

1. `EnterWorktree` でタスク用ワークツリーを作成
2. 作業対象ファイルのみを Read / Edit（不要なファイルは開かない）
3. 完了後に main へマージ
4. ワークツリーを削除（`git worktree remove` または `ExitWorktree`）

---

## 禁止事項

- main への直接コミット（緊急の1行修正を除く）
- 常設の機能別ワークツリーの作成
- タスク完了後のワークツリー放置（prunable 状態を残さない）

---

## Claude Code / Codex 共通

- コンテキスト消費を最小化するため、Glob/Grep でピンポイント検索してから必要なファイルのみ開く
- 作業外のディレクトリへの広域探索は行わない
- `.codexignore` に従い `dist/` や `*.png` は原則開かない
